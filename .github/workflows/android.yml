name: Build Android APK (GPS Map Camera)

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - uses: actions/setup-node@v4
        with:
          node-version: 16

      # 3️⃣ Setup Java (for Gradle)
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      # 4️⃣ Install npm dependencies
      - name: Install dependencies
        run: npm install

      # 5️⃣ Remove old android folder (clean build)
      - name: Clean android
        run: rm -rf android

      # 6️⃣ Add Android platform
      - name: Add Android
        run: npx cap add android

      # 7️⃣ 🔥 Inject ALL permissions into AndroidManifest.xml
      - name: Add Android permissions
        run: |
          MANIFEST=android/app/src/main/AndroidManifest.xml

          sed -i '/<manifest/a\
          \    <uses-permission android:name="android.permission.INTERNET" />\
          \    <uses-permission android:name="android.permission.CAMERA" />\
          \    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />\
          \    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />\
          \    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />\
          \    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />' $MANIFEST

      # 8️⃣ Sync Capacitor
      - name: Capacitor sync
        run: npx cap sync android

      # 9️⃣ Build APK
      - name: Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      # 🔟 Upload APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: GPS-Map-Camera-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
